{% extends 'base.html.twig' %}

{% block stylesheets %} <link rel="stylesheet" href="historiqueReunion.css">{% endblock %}

{% block body %}


<section class ="articles">
        <article>
            <h1>{{ reunion.titre }}</h1>
                <div class="metadata">
                    Ecrit le {{ reunion.createAt | date('d/m/Y') }} à {{ reunion.createAt | date('H:i') }}
                </div>
                <div class="content">
                    <div class="title">Description de la réunion : </div>
                    <div>{{ reunion.description }}</div>
                    <div class="title">Pour la prochaine fois : </div>
                    <div>{{ reunion.afaire | raw }}</div>
                </div>
        <article>

</section>

{% endblock %}